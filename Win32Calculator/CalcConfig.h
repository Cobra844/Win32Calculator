#pragma once

// Configuration:

#define DebugToggle 0 // (1 = Enable, 0 = Disable)

#define WindowMenuName TEXT("Calculator") // Change the window name

#define WindowStartingXPosition 0 // Change the X Position of window when first initialized
#define WindowStartingYPosition 0 // Change the Y Position of window when first initialized
#define WindowStartingXLength 516 // Change the X Length of window when first initialized
#define WindowStartingYLength 489 // Change the Y Length of window when first initialized

#define Button0MenuName TEXT("0") // Change the text shown in Button 0
#define Button1MenuName TEXT("1") // Change the text shown in Button 1
#define Button2MenuName TEXT("2") // Change the text shown in Button 2
#define Button3MenuName TEXT("3") // Change the text shown in Button 3
#define Button4MenuName TEXT("4") // Change the text shown in Button 4
#define Button5MenuName TEXT("5") // Change the text shown in Button 5
#define Button6MenuName TEXT("6") // Change the text shown in Button 6
#define Button7MenuName TEXT("7") // Change the text shown in Button 7
#define Button8MenuName TEXT("8") // Change the text shown in Button 8
#define Button9MenuName TEXT("9") // Change the text shown in Button 9

#define PlusButtonMenuName TEXT("+")
#define MinusButtonMenuName TEXT("-")
#define MultiplyButtonMenuName TEXT("*")
#define DivideButtonMenuName TEXT("/")
#define EqualButtonMenuName TEXT("=")

#define UseNumPadKeys true // (true = Enable, false = Disable)